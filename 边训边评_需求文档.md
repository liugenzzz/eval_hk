# 边训边评（LLaMA-Factory → eval_tool）— 需求梳理

状态：**已被 `边训边评_设计文档.md` 取代**。本文保留为需求梳理阶段的记录；
其中第五、九节的待确认项已全部拍板，结论以设计文档为准。
日期：2026-09-08
负责范围：本文只定义**评估侧**的入参、出参、触发方式和实施步骤；训练侧只需按契约产出一个信号文件。

---

## 〇、一句话结论

**你要做的不是"再写一个评估器"，而是给已有的 `eval_tool` 加一层"接单器"。**

现有 `python -m eval_tool infer/eval --config pipeline.json` 已经能完整跑通
推理 → 裁判 → 报告，并且支持断点续传、裁判缓存、baseline 复用。
边训边评缺的只有四件事：

1. **一个触发契约**（LLaMA-Factory 存完 checkpoint 后怎么通知你）
2. **一个最小入参集**（训练侧到底要给你哪几个字段）
3. **一个队列/调度器**（GPU 只有那么多，评估不能和训练抢崩）
4. **一张按 step 累积的趋势表**（现在的 `score_summary.csv` 是"每模型一行"，没有 step 维度）

所以下面的重点是**契约**和**约束**，不是评分算法——评分口径完全复用现状，不要动。

---

## 一、边界划分（先把这个谈死，后面才不扯皮）

| 事项 | 训练侧（LLaMA-Factory） | 评估侧（你） |
|------|------------------------|-------------|
| checkpoint 产出与"写完"的判定 | ✅ 负责，并给出明确的完成信号 | ❌ 不猜测、不轮询文件大小 |
| LoRA adapter 合并成完整权重 | ✅ 建议由训练侧做（见 Q2） | ⚠️ 若由评估侧做，需新增能力 |
| 训练元信息（step/epoch/数据版本） | ✅ 负责写进信号文件 | ❌ 不从日志里反解 |
| 评估集、提示词、裁判模型、rubric | ❌ 不参与 | ✅ 完全由评估侧固化 |
| 评估执行、断点、缓存、报告 | ❌ | ✅ |
| 结果回传格式与趋势表 | ❌ 只消费 | ✅ 负责产出 |
| GPU 资源分配 | 🤝 共同约定（见 Q1，最大风险项） | 🤝 |
| 是否因分数下降而早停 | ✅ 决策权在训练侧 | ✅ 只提供信号，不代做决策 |

**关键原则：训练侧只给"这是哪个模型 + 训练到哪了"，其余全部是评估侧的固化配置。**
理由：只有把评估集、提示词、裁判、rubric 全部冻结在评估侧，不同 step 的分数才可比。
一旦允许训练侧动态传入评估参数，趋势线立刻失去意义。

---

## 二、现有能力盘点（能直接复用的，别重写）

| 能力 | 现状 | 位置 |
|------|------|------|
| 统一配置 + 子命令 | ✅ 已有 `convert/infer/eval/sweep/all` | `eval_tool/cli.py` |
| 多模型一次跑完 | ✅ `models[]` 列表 | `eval_tool/config.py:PipelineConfig` |
| 预测路径自动推导 | ✅ `work_dir/<name>/<name>_<dataset>.xlsx` | `eval_tool/artifacts.py` |
| 推理断点续传 + 指纹校验 | ✅ 分片 JSONL + manifest | `eval_tool/infer_cache.py`、`run_infer.py` |
| 裁判缓存（换 prompt 自动失效） | ✅ `judge_fp` 参与缓存键 | `eval_tool/cache.py`、`run_eval.py:33` |
| baseline 免重判 | ✅ `models[].scored` 直读上次 detail | `config.py:ModelConfig.scored_paths` |
| 抽样限量 | ✅ `infer.limit` | `config.py:InferConfig.limit` |
| 点式打分 / 成对胜率 / 置信区间 / 长度控制 | ✅ | `score_vqa.py`、`aggregate.py` |
| 总分表 | ✅ `score_summary.csv`（total_score + 各维度） | `report.py:40` |
| 离线全链路自检 | ✅ 假模型 + 假裁判 | `e2e_check.py` |

### 缺口清单（本项目真正要新写的）

| # | 缺口 | 说明 | 优先级 |
|---|------|------|--------|
| G1 | **触发器/接单器** | 监听 checkpoint 信号 → 生成本次 run 的 config → 调 `run_inference`/`run_evaluation` | P0 |
| G2 | **任务队列 + 幂等** | 同一 checkpoint 重复触发只跑一次；评估慢于保存时排队而不是并发 | P0 |
| G3 | **GPU 互斥/让路** | 与训练进程共卡时的显存与调度策略 | P0 |
| G4 | **step 维度趋势表** | 现有报表没有 step/epoch 列，无法画曲线 | P0 |
| G5 | **结果回传** | 写回调文件 / HTTP callback / 落库 | P1 |
| G6 | **LoRA adapter 支持** | 代码审计确认：`eval_tool/infer.py` 只用 `AutoProcessor/from_pretrained` 加载**完整**模型目录，**没有任何 peft/adapter 分支** | P0（若训练是 LoRA） |
| G7 | **回归集（防遗忘）** | 现在只有领域集；加了领域数据后通用能力是否掉，需要第二个集合 | P1 |
| G8 | **失败重试与告警** | 裁判服务超时、OOM、checkpoint 不完整 | P1 |

---

## 三、接口契约 A：训练侧 → 评估侧（入参）

### 3.1 传递方式（推荐：文件即接口）

训练侧在 checkpoint **完全写完之后**，在约定目录写一个 sidecar JSON：

```
<signal_dir>/<run_id>__step<step>.json      # 内容见 3.2
<signal_dir>/<run_id>__step<step>.json.done # 空文件，最后写，作为"写完"的原子标志
```

评估侧只扫 `*.json.done`，看到才读对应 json。**不要靠文件 mtime 或大小判断写完**。

选它的理由：训练侧改动最小（一个 `TrainerCallback` 里 5 行），跨机器可用（共享存储/NFS），
天然可重放、可审计、可手工补单。HTTP 方案见第五节对比。

### 3.2 字段定义

#### 必填（训练侧必须给，缺一不可）

| 字段 | 类型 | 示例 | 为什么必须 |
|------|------|------|-----------|
| `request_id` | string | `"zbv3_20260908_step600"` | **幂等键**。评估侧按它去重，重复投递直接跳过 |
| `run_id` | string | `"qwen3vl8b_zb_sft_v3"` | 一次训练任务的标识，趋势表按它分组 |
| `step` | int | `600` | 趋势表横轴，必须单调 |
| `checkpoint.path` | string(绝对路径) | `"/code/output/zb_v3/checkpoint-600"` | 评估侧要加载的东西 |
| `checkpoint.kind` | enum | `"full"` / `"adapter"` | 决定评估侧能否直接加载，见 G6/Q2 |

#### 强烈建议（不给会导致报表缺信息或口径不明）

| 字段 | 类型 | 示例 | 用途 |
|------|------|------|------|
| `epoch` | float | `1.5` | 报表第二横轴；断点续训后 step 可能不连续 |
| `checkpoint.base_model` | string | `"/models/Qwen3-VL-8B-Instruct"` | `kind=adapter` 时必填；同时作为 base 对照 |
| `checkpoint.saved_at` | RFC3339 | `"2026-09-08T12:00:00+08:00"` | 排队与时序审计 |
| `is_final` | bool | `false` | 最后一个 checkpoint 走"全量评估"，中间的走"抽样评估" |
| `total_steps` | int | `2000` | 估算还会来多少单，用于队列容量规划 |
| `train_meta.data_version` | string | `"zb_equip_v3"` | **你说的"按这批领域数据的加入去评估"，靠这个字段区分** |
| `train_meta.dataset_tags` | string[] | `["zb_equip_v3","general_mix"]` | 记录本次混了哪些数据 |
| `train_meta.stage` | enum | `"sft"`/`"dpo"`/`"pt"` | 影响默认 rubric 与对照组 |
| `train_meta.finetuning_type` | enum | `"full"`/`"lora"` | 与 `checkpoint.kind` 交叉校验 |

#### 可选（评估侧有默认值，训练侧一般不填）

| 字段 | 默认 | 说明 |
|------|------|------|
| `eval.suite` | `"zb_equip_online"` | 指向评估侧固化的一份 `pipeline.json`。**不允许训练侧传具体评估参数** |
| `eval.limit` | 由 suite 决定（建议 200） | 在线抽样条数；`is_final=true` 时忽略，跑全量 |
| `eval.priority` | `"normal"` | `high` 可插队（如 final checkpoint） |
| `callback_url` | null | 给了就 POST 结果；不给只落盘 |

#### 完整示例

```json
{
  "request_id": "zbv3_20260908_step600",
  "run_id": "qwen3vl8b_zb_sft_v3",
  "step": 600,
  "epoch": 1.5,
  "is_final": false,
  "total_steps": 2000,
  "checkpoint": {
    "path": "/code/output/zb_v3/checkpoint-600",
    "kind": "full",
    "base_model": "/code/models/Qwen3-VL-8B-Instruct",
    "saved_at": "2026-09-08T12:00:00+08:00"
  },
  "train_meta": {
    "stage": "sft",
    "finetuning_type": "full",
    "data_version": "zb_equip_v3",
    "dataset_tags": ["zb_equip_v3", "general_mix"],
    "train_samples": 12000,
    "learning_rate": 1e-5
  },
  "eval": {
    "suite": "zb_equip_online",
    "limit": 200,
    "priority": "normal"
  },
  "callback_url": null
}
```

### 3.3 评估侧如何把它翻译成现有配置

评估侧维护一份**模板** `suites/zb_equip_online.json`（就是现在的 `pipeline.json`），
接单后只替换四处，其余原样不动：

| pipeline.json 字段 | 由请求推导 |
|--------------------|-----------|
| `models[]` | `[{name: "base", scored: {...上次结果}}, {name: "<run_id>_step<step>", model_path: checkpoint.path}]` |
| `work_dir` | `<work_root>/<run_id>/step-<step>` |
| `out_dir` | `<report_root>/<run_id>/step-<step>` |
| `infer.limit` | `is_final ? null : eval.limit` |

`cache_dir` **必须全局共享一份**（`<cache_root>/<suite>`），这样 baseline 和重复条目的裁判结果跨 step 复用，
省下大量裁判 API 调用。`tsv_dir`、`prompts/*`、`judge.*`、`rubric`、`seed`、`category_weights` 一律**不允许按 step 变**。

---

## 四、接口契约 B：评估侧 → 训练侧/看板（出参）

### 4.1 单次结果（每个 checkpoint 一个 JSON）

写到 `<report_root>/<run_id>/step-<step>/eval_result.json`，同时（可选）POST 到 `callback_url`。

```json
{
  "request_id": "zbv3_20260908_step600",
  "run_id": "qwen3vl8b_zb_sft_v3",
  "step": 600,
  "epoch": 1.5,
  "model_name": "qwen3vl8b_zb_sft_v3_step600",
  "status": "succeeded",
  "metrics": {
    "total_score": 0.7213,
    "n": 200,
    "by_category": { "单轮": 0.7502, "多轮": 0.6810 },
    "ci95": [0.6802, 0.7594],
    "pred_len_mean": 486.2
  },
  "vs_baseline": {
    "baseline_model": "base",
    "delta_total_score": 0.0841,
    "win": 0.58, "tie": 0.19, "loss": 0.23,
    "win_rate_lc": 0.55
  },
  "regression": {
    "suite": "general_holdout",
    "total_score": 0.6102,
    "delta_vs_base": -0.0210,
    "alert": false
  },
  "artifacts": {
    "report_dir": "/code/eval_report/zbv3/step-600",
    "score_summary_csv": ".../score_summary.csv",
    "judge_detail_all_xlsx": ".../judge_detail_all.xlsx",
    "warnings_log": ".../warnings.log"
  },
  "fingerprint": {
    "suite": "zb_equip_online",
    "rubric": "v4",
    "judge_fp": "…",
    "tsv_sha256": "…",
    "eval_tool_commit": "…",
    "sampled": true, "limit": 200, "sample_seed": 42
  },
  "timing": {
    "queued_at": "…", "started_at": "…", "finished_at": "…",
    "infer_seconds": 412, "judge_seconds": 96
  },
  "error": null
}
```

**`status` 取值**：`succeeded` / `failed` / `skipped`（重复请求或被降采样丢弃）/ `partial`（部分数据集缺预测）。
**`fingerprint` 是可比性的凭证**：两条记录的 `judge_fp + tsv_sha256 + rubric` 不同就不能画在同一条曲线上，看板要据此拒绝或分色。

### 4.2 趋势表（新增，G4）

`<report_root>/<run_id>/eval_trend.csv`，**每次评估追加一行**，列：

```
run_id, step, epoch, model_name, status, n, sampled,
total_score, ci95_low, ci95_high, 单轮, 多轮,
delta_vs_base, win, tie, loss,
regression_score, regression_delta, regression_alert,
data_version, judge_fp, rubric, finished_at
```

这张表就是"边训边评"的最终产物：训练侧看它决定要不要继续训、选哪个 checkpoint。

### 4.3 早停/选优信号（只给建议，不代做决策）

在趋势表基础上额外输出 `best_checkpoint.json`：当前 `run_id` 下 `total_score` 最高
且 `regression_alert=false` 的那一步。建议判定规则（可调）：

- **提升**：`delta_vs_base > 0` 且 95% CI 下界 > 0
- **回归告警**：`regression.delta_vs_base < -0.03`（通用能力掉超过 3 个点）
- **平台期**：连续 3 个 checkpoint 的 `total_score` 提升 < 0.005

---

## 五、触发与集成方案对比

| 方案 | 训练侧改动 | 跨机 | 可重放 | 复杂度 | 结论 |
|------|-----------|------|--------|--------|------|
| **A. 目录信号文件 + 评估侧轮询** | `TrainerCallback.on_save` 写两个文件 | ✅ 共享存储即可 | ✅ | 低 | **推荐（一期）** |
| B. 评估侧起 HTTP 服务，训练侧 POST | 同上，改成 requests.post | ✅ | ❌ 需自己补幂等与重试 | 中 | 二期可加，与 A 共存 |
| C. 消息队列（Redis/RabbitMQ） | 引入依赖 | ✅ | ✅ | 高 | 单机单训练任务不值得 |
| D. LLaMA-Factory 内嵌调用评估 | 侵入训练进程 | ❌ | ❌ | 中 | **反对**：评估崩了会拖垮训练，且抢同一进程的显存 |

一期做 A。信号目录用共享盘，评估侧一个常驻进程 `python -m eval_tool serve --watch <signal_dir>`（新增子命令）。
轮询间隔 30s 足够——checkpoint 保存间隔通常在十分钟以上。

---

## 六、关键工程约束（这几条不解决，做出来也跑不稳）

### C1 GPU 争用 —— 最大风险，必须先拍板
训练在占卡，评估推理同样要卡，裁判如果也是本地 27B 服务还要再占一份。三者抢同一批卡 = 训练 OOM 或双双变慢。
可选口径（见 Q1）：
- **独立评估卡**（最优）：`infer.gpu_ids` 指定训练不用的卡，互不干扰。
- **共卡串行**：评估期间暂停训练——不现实，否决。
- **共卡限显存**：给评估进程设 `CUDA_MPS`/显存上限，风险仍在，只能作为兜底。
- **离线补评**：训练时只入队不评估，训练结束后批量跑——那就不是"边训边评"了，但作为降级预案要保留。

### C2 checkpoint 就绪判定
LLaMA-Factory 保存是多文件写入，读到一半的目录会加载失败或产生错误权重。
**契约里的 `.done` 标志文件是硬性要求**，评估侧再加一层防御：检查 `config.json` + 权重文件（`*.safetensors` 与 `model.safetensors.index.json` 中声明的分片数一致）+ processor/tokenizer 文件齐全，缺一即 `failed`，不静默降级。

### C3 评估耗时 vs 保存频率
若单次评估耗时 > `save_steps` 的实际间隔，队列会无限堆积。策略（建议默认）：
**队列只保留最新一个待评估 checkpoint**，中间的标 `skipped`（`is_final=true` 的除外，必评）。
一期先测出单次耗时 T，再倒推 `save_steps`。粗算：`T ≈ (limit × 单条推理耗时) + (limit × 裁判耗时 / max_workers)`。

### C4 可比性冻结
同一个 `run_id` 内，评估集 TSV、`prompts/*`、裁判模型与温度、rubric、抽样 seed 必须完全不变。
一期就把 `tsv_sha256` 和 `judge_fp` 写进结果，出现变化时**报错中止**而不是继续画曲线（与现有推理指纹的处理方式一致）。

### C5 抽样口径
`infer.limit` 现在是 `data.head(limit)`（`run_infer.py:67-68`），是**取前 N 条**而不是随机抽样。
边训边评如果只跑前 200 条，会系统性偏向数据集前部（可能按类目排序）。
**需要新增固定种子的分层抽样**，并把抽中的 index 列表落盘，保证每个 step 评的是同一批题。

### C6 幂等与并发
- 同一 `request_id` 已有 `eval_result.json` 且 `status=succeeded` → 直接跳过。
- 常驻进程加文件锁，防止手工命令与 watcher 同时跑同一个 `work_dir`。
- `work_dir` 按 step 分目录，避免不同 step 的推理分片互相污染。

### C7 LoRA（若适用）
现有推理路径不认 adapter。二选一：训练侧 `export_model` 出完整权重（推荐，评估侧零改动），
或评估侧新增"加载 base + apply adapter"能力（要引 `peft`，且合并后磁盘占用按 checkpoint 数量线性增长）。

---

## 七、评估内容设计（对应"按这批领域数据的加入去评估"）

一个 checkpoint 建议同时跑两个集合：

| 集合 | 内容 | 规模建议 | 作用 |
|------|------|---------|------|
| **领域集** `zb_equip_online` | 与本次加入的领域数据同分布、但**严格不重叠**的 held-out | 在线 200，final 全量 | 证明新数据学进去了 |
| **回归集** `general_holdout` | 训练前就有的通用/旧领域题 | 100~150 | 证明没学崩、没灾难性遗忘 |

两条曲线一起看才有意义：领域分涨、回归分不掉 → 这批数据加得对；领域分涨、回归分掉 → 过拟合或配比有问题。

**必须核对训练集与评估集无重叠**（按题目文本或图片哈希去重），否则分数虚高。这一步在数据准备阶段做一次即可，
但要把去重结论写进 suite 的说明里。

对照组固定为 `baseline_model: "base"`（训练起点权重），且用 `models[].scored` 复用它的历史评分结果，
每个 step 不重复调裁判。

---

## 八、实施步骤与里程碑

| 里程碑 | 交付物 | 验收标准 | 依赖 |
|--------|--------|---------|------|
| **M0 契约冻结** | 本文档 + `eval_request.schema.json` / `eval_result.schema.json` | 训练侧签字确认字段表；双方各写一个样例文件互相校验 | Q1-Q5 拍板 |
| **M1 手工跑通** | 用一个真实 checkpoint 手工跑 `infer`+`eval`，记录耗时 T | 出 `score_summary.csv`；T 与 `save_steps` 的关系明确 | GPU 到位 |
| **M2 抽样与趋势表** | 分层抽样（C5）+ `eval_trend.csv`（G4）+ `eval_result.json`（G5） | 同一 checkpoint 跑两次，抽样 index 与分数完全一致 | M1 |
| **M3 接单器** | `eval_tool serve --watch`：扫描 → 校验 → 队列 → 执行 → 回写 | 手工丢 3 个信号文件（含 1 个重复、1 个坏 checkpoint），行为符合 C2/C3/C6 | M2 |
| **M4 训练侧对接** | LLaMA-Factory `TrainerCallback` 写信号文件 | 一次真实训练全程无人工干预，趋势表逐 step 增长 | M3 + 训练侧排期 |
| **M5 加固** | 回归集、失败重试、告警、`best_checkpoint.json` | 断裁判服务/断电重启后能自动恢复 | M4 |

**M0→M1 是纯确认工作，不写代码，但决定了后面全部返工量，先做。**
M2 之前不要碰 M3——没有稳定的抽样和趋势表，接单器跑起来只会积累不可比的数据。

---

## 九、待拍板问题（我给了建议默认值，请逐条确认或推翻）

| # | 问题 | 我的建议 | 谁来定 |
|---|------|---------|--------|
| **Q1** | 评估用哪些 GPU？和训练共卡还是独占？ | **独占 1~2 张训练不用的卡**；无独立卡则降级为"训练结束后离线补评" | 训练侧 + 你 |
| **Q2** | 训练是 full 还是 LoRA？adapter 谁来合并？ | LoRA 时由**训练侧 `export_model` 出完整权重**，评估侧零改动 | 训练侧 |
| **Q3** | 裁判服务（qwen3.6-27b）部署在哪？会和训练抢卡吗？ | 部署在**独立机器/独立卡**，否则评估会拖慢训练 | 你 + 运维 |
| **Q4** | 在线评估抽多少条？`save_steps` 设多少？ | 先按 **200 条 / 每 epoch 存 2~3 次**试跑，M1 实测后再定 | 你 + 训练侧 |
| **Q5** | 评估慢于保存时，堆积的 checkpoint 怎么办？ | **只留最新一个，中间标 `skipped`**；`is_final` 必评 | 你 |
| **Q6** | 结果给谁看、以什么形式？ | 一期只出 `eval_trend.csv` + `eval_result.json`；看板/曲线二期 | 你 + 需求方 |
| **Q7** | 是否需要回归集（防遗忘）？ | **需要**，否则无法判断领域数据的净收益 | 你 + 需求方 |
| **Q8** | 训练侧能否接受写信号文件？还是只能提供日志/HTTP？ | 优先信号文件（方案 A） | 训练侧 |
| **Q9** | 评估失败是否影响训练？ | **绝不影响**：评估进程完全独立，失败只记录不回传中断信号 | 双方 |
| **Q10** | 中间 checkpoint 是否做成对比较（pairwise）？ | **在线只做点式**（pairwise 裁判调用量翻倍）；final 才做 pairwise | 你 |

---

## 十、风险登记

| 风险 | 影响 | 缓解 |
|------|------|------|
| GPU 争用导致训练 OOM 中断 | 🔴 高：白烧几小时训练 | Q1 定独立卡；评估进程独立，可随时 kill |
| checkpoint 读到写了一半的目录 | 🔴 高：错误权重 → 错误分数 → 错误决策 | `.done` 标志 + 文件完整性校验（C2） |
| 抽样取前 N 条造成系统性偏差 | 🟠 中：曲线不代表真实能力 | C5 分层抽样 + 固定 index 列表 |
| 中途改了提示词/评估集 | 🟠 中：曲线不可比但看不出来 | fingerprint 写入结果 + 变化即报错（C4） |
| 训练集与评估集重叠 | 🟠 中：分数虚高 | 数据准备阶段哈希去重并留证 |
| 裁判服务不稳/超时 | 🟡 低：单点失败可重试 | 裁判缓存已有；加重试与 `failed` 状态，不污染趋势表 |
| 评估堆积拖成"训练完还在评" | 🟡 低 | C3 只留最新 + `skipped` |

---

## 附：对现有代码的改动预估（仅供排期参考）

| 文件 | 改动 |
|------|------|
| `eval_tool/serve.py` | **新增**：watcher + 队列 + 幂等 + 请求校验（G1/G2/C6） |
| `eval_tool/request.py` | **新增**：请求/结果 dataclass 与 schema 校验（契约 A/B） |
| `eval_tool/trend.py` | **新增**：`eval_trend.csv` 追加与 `best_checkpoint.json`（G4） |
| `eval_tool/config.py` | 由 suite 模板 + 请求派生 `PipelineConfig`（3.3） |
| `eval_tool/run_infer.py` | `limit` 改为固定种子分层抽样并落盘 index（C5） |
| `eval_tool/cli.py` | 新增 `serve` 子命令 |
| `eval_tool/infer.py` | 仅当 Q2 选"评估侧合并 adapter"时才动（G6） |
| 测试 | 幂等、坏 checkpoint、队列丢弃、抽样一致性、趋势表追加 |

**不动**：`judge.py`、`judge_rubrics.py`、`score_vqa.py`、`aggregate.py`、`report.py`、`prompts/*`。
评分口径保持不变，这是趋势可比的前提。
