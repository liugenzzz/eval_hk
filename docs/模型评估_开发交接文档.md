# 模型评估 — 开发交接文档

**版本** v1 · 2026-09-01
**代码分支** `claude/model-eval-grounding`（已推送）
**被测对象** Qwen3-VL-8B-Instruct 的 SFT checkpoint，对比 base
**需求正本** `模型评估_需求文档.md`（在 `liugenzzz/target_detection_vl_dataset` 仓库的 `qwen3vl_sft_builder/docs/`）

> 本文档面向**接手开发的人**。需求「为什么这么定」在正本里，这里只讲**现在做到哪、接下来怎么做、怎么算做完**。
> 每一条规格后面括号里的 §N 指向需求正本的对应章节。

---

## 0. 一句话背景

评的是**训练出来的 checkpoint**，不是数据集。三个问题：SFT 后定位能力涨了多少（对比 base）、有没有牺牲别的能力、**哪一档/哪一类/哪种尺寸还不行**。

第三点是这次评估的唯一真正产出。**只出一个总分没有用** —— 所有设计都服务于「能拆开看」。

---

## 1. 已完成（阶段 0 / 1）

提交 `3af6bd7`，767 行新增。**测试从 703 → 733 passed / 5 skipped，现有测试断言一行未改。**

### 1.1 `eval_tool/scorers.py` — 打分器注册表

重构前 `run_eval.py` 里是写死的：

```python
for dataset_key in ("mcq", "judge", "vqa"):     # 硬编码三元组
    if dataset_key in {"mcq", "judge"}:         # 硬编码分支
```

`datasets` 的键名本来是自由的，但打分逻辑写死了这三个 —— **自定义键连循环都进不去，静默被忽略**。

现在：数据集声明 `kind`，打分器按 kind 查表分派。

```python
@register("grounding_single")
def score_x(data: pd.DataFrame, ctx: ScoreContext) -> pd.DataFrame: ...
```

`ScoreContext` 统一携带 `dataset_key / model_name / config / judge_client / pointwise_cache / image_map`，各取所需。

**向后兼容**：未声明 kind 时按键名推断（`mcq`/`judge` → `choice`，其余 → `judge_text`），和原来的 if/else 一字不差。

### 1.2 `eval_tool/config.py` — `dataset_kinds`

两个配置类都加了 `dataset_kinds: dict[str, str]` 和 `kind_of(key)`。加载时校验 kind 是否已注册 —— **拼错的话跑到一半才炸，那时推理已经花掉了**。

### 1.3 `eval_tool/bbox.py` — 解析与几何

- `parse(text) -> ParsedBoxes`：从模型输出里捞框。覆盖空输出、非 JSON、坐标顺序颠倒、越界、退化成点、` ```json ` 围栏、JSON 前后带解释性文字（正则兜底）。
- `iou(a, b)`
- `deviations(pred, gt)`：四个角点的**有符号**偏差 + `mae_4pt` + `dev_max`

`ParsedBoxes.flags` 记录修过什么（`code_fence` / `reversed` / `out_of_range` / …）。**这些只计数不扣分** —— base 模型爱自己加围栏，扣分的话测出来的是格式差异不是能力差异（§9）。

### 1.4 `eval_tool/score_grounding.py` — `grounding_single` / `grounding_multi`

已实现 §3、§4 的全部规则，详见 §2.1 的口径速查。

---

## 2. 全局硬约束（验收标准，每个阶段都适用）

| # | 约束 | 怎么验 |
|---|---|---|
| 1 | **`engine: code` 的打分器不许调任何模型**，一行网络请求都不能有 | `tests/test_bbox.py::test_scorers_never_touch_the_network` 封掉 socket 后仍能打分 |
| 2 | **同一份预测重跑一百遍，数字逐位相同** | `test_scorers_are_deterministic` |
| 3 | **提示词全部外置、带版本号**，配置里按文件名引用 | 沿用现有 `prompts/judge_*_v1..v4b` 命名 |
| 4 | **现有测试断言不许改** | `git diff --stat -- tests/` 对已有文件为空 |
| 5 | **坐标反归一化只有一个实现** | 对齐 `bbox.py`，不许第二份 |

第 4 条特别说明：**新增测试文件随便加，但改动已有测试文件里的断言 = 重构改变了行为，打回。**

### 2.1 坐标口径速查（§3、§4）

```
偏差一律在 [0, 1000] 归一化空间算，不在像素空间
  像素空间下 2048 宽的图差 20px 和 128 宽的图差 20px 不是一回事

主指标   MAE_4pt                四个坐标各自的绝对偏差再平均
        dev_x1/y1/x2/y2        四个点分开，看得出哪条边不准
        bias_*（有符号均值）    x1 正而 x2 负 = 系统性框小 ← 最有诊断价值
        Acc@dev<=20 / <=50     四个点【全部】不超过 N（取最差角，不是平均）

副指标   Acc@IoU0.5 / 0.75 / mIoU   用于和 RefCOCO 系列横向比

【关键】偏差只在 IoU >= 0.5 的样本上算。IoU < 0.5 记为「定位失败」单独计数。
        报表必须同时出【定位成功率】和【成功样本上的四点偏差】两个数，
        不许合成一个 —— 只报后者会漂亮得离谱，只报前者又丢了精度信息。

多框     匈牙利匹配（IoU 代价，阈值 0.5）-> 配上的算偏差
        配不上的分漏检（gold 有 pred 没有）/ 误检（pred 有 gold 没有）
        P / R / F1@0.5 + 数量准确率 + 数量 MAE
        （准确率说「多少张图数对了」，MAE 说「数错时错多少」——
          错 1 个是边界判断，错 10 个是压根没在数）
```

> **禁止项：不做 COCO 的 `AP@[.5:.95]`。** 模型输出纯 JSON 坐标、没有置信度，硬上 `pycocotools` 只能给所有框填 1.0，算出来是 F1 的变形，还多一层解释成本。**看到有人装 pycocotools 就是走错路了。**

---

## 3. 数据接入 —— 优先级最高的一块

### 3.1 输入长什么样

数据集项目产出 `output/test.jsonl`，ShareGPT 多轮：

```json
{
  "id": "xxx_ground_appearance_0",
  "images": ["cvdata_clean01_000216.jpg"],
  "conversations": [
    {"from": "human", "value": "<image>\n框出图中那辆白色面包车。"},
    {"from": "gpt",   "value": "{\"bbox_2d\": [412, 233, 566, 388]}"},
    {"from": "human", "value": "这辆车本身长什么样？"},
    {"from": "gpt",   "value": "深红色车身，支着一顶白色遮阳篷…"}
  ],
  "metadata": {
    "task_type": "ground_appearance", "is_main_line": true,
    "difficulty": "medium", "size_bucket": "small",
    "area_ratio": 0.0031, "equiv_px": 57.7,
    "label": "面包车", "describe_kind": "appearance",
    "focus_box_indices": [3], "n_turns": 2,
    "source_image": "...", "image_width": 1920, "image_height": 1080,
    "coordinate_mode": "qwen_relative_1000", "bbox_scale": 1000
  }
}
```

- **金标框就在 `gpt` 轮里**，已经是 `[0,1000]` 归一化的。
- `images` 是**裸文件名**（数据集项目的 `output.image_path_style: filename`），要配 `images_dir` 还原成绝对路径。

### 3.2 【坑 1】现有 convert 会把 metadata 整个丢掉

`eval_tool/convert_vqa_json.py:151` 的输出列写死九列：

```python
fields = ["index", "image", "question", "answer", "history",
          "category", "l2-category", "split", "source_id"]
```

`metadata` 不在里面。后果两个，都是致命的：

1. **报表五个维度一个都拆不开**（§16 要按 `task_type` / `difficulty` / `size_bucket` / `label` / `describe_kind` 拆）—— 而「能拆开看」是这次评估的唯一真正产出。
2. **打分器不知道该用哪种打分方式** —— `dataset_kinds` 是按数据集（按 TSV 文件）声明的，但一份 `test.jsonl` 里混着 14 种 `task_type`、5 种答案形态。

**好消息**：`io.py:72` 的 `align_truth_and_prediction` 是 `truth.merge(pred, on="index")`，**真值侧的额外列会原样保留**，一路能活到报表。所以只要 convert 写出来就行，不用改下游。

### 3.3 要做的：`convert_grounding`

新写一个转换器（**不要改 `convert_vqa_json.py`**，那条路给通用问答用，动了会连坐）：

1. **按答案形态分流成 5 份 TSV**，每份在配置里声明自己的 `kind`：

   | dataset | task_type | kind |
   |---|---|---|
   | `det_single` | `ground_unique` + 7 个 `ground_*` 的**第 1 轮** | `grounding_single` |
   | `det_multi` | `detect_class`、`inventory_locate` | `grounding_multi` |
   | `short_ans` | `attribute_qa`、`spatial_relation` | `short_answer`（阶段 2 写） |
   | `describe` | 主线**第 2 轮**（7 种 describe kind） | `describe`（阶段 6 写） |
   | `object_ident` | `region_identify` | `object_ident`（阶段 2 写） |
   | `refuse` | `exist_negative` | `exist_negative`（阶段 2 写） |

2. **metadata 带成额外列**，统一加 `meta_` 前缀：`meta_task_type` / `meta_difficulty` / `meta_size_bucket` / `meta_label` / `meta_describe_kind` / `meta_is_main_line`。

3. **`images_dir` 必填**，缺了直接报错 —— 不要跑到推理阶段才发现图读不到。

4. 多轮拆分沿用现有约定：当前轮的问题进 `question`，之前的轮进 `history`。**但要支持 §7 的两种 history 模式**（见阶段 5）。

### 3.4 【坑 2】像素预算必须和训练对齐

`infer` 块的 `image_min_pixels` / `image_max_pixels` 必须与训练时的 LLaMAFactory 配置一致（README 已写明 **65536 / 589824**）。

分辨率变了，坐标虽是归一化的不会错位，但**模型的空间精度会变，数字不可比**。

**要求：配置加载时硬校验，不一致直接报错。不许只写在文档里。**（§17.4）

### 3.5 要不要读原始 label 和图片

| 东西 | 要不要 | 为什么 |
|---|---|---|
| **图片** | **必须** | 推理时模型要看图；且要按 §3.4 预缩放 |
| **YOLO label `.txt`** | **只有 CHAIR 要** | CHAIR 要「整张图的真值类别集合」，而 `test.jsonl` 每条只带本样本聚焦的 `label`。用同图样本的 label 并集近似会**高估**幻觉率（并集小于真实集合，模型提到真实存在但没进并集的类别会被误判成幻觉） |
| **`classes.yaml`（248 类）** | **必须** | ① E 组上下位判定 ② CHAIR 的类别词表 |
| **原图尺寸做反归一化** | **不要** | 坐标已是归一化的，读原图反而引入「用哪个尺寸」的新错误源 |

---

## 4. 剩余阶段

顺序有依赖，**阶段 2 和 3 之间无依赖可并行**。

### 阶段 2 — C / E / 拒答三组打分器（§5.1、§6）

**`short_answer`**：归一化后精确匹配；不匹配时**才**丢给裁判判一次是不是同义。

**`object_ident`**（物体识别）：三档判定，用 `classes.yaml` 的上下位表

| 判定 | 条件 | 计入 |
|---|---|---|
| 精确命中 | 归一化后完全一致 | 主指标 |
| **上位命中** | 答案是金标的上位词（答「三轮车」对金标「遮阳三轮车」） | **单独计数** |
| 错误 | 其余 | — |

> **主指标报精确命中率，上位命中率作为第二个数，不许合成。** 合成会掩盖一个真实问题：模型可能学会了「答粗一点更安全」，那是退化不是能力。

答案不在类别表里（模型自创了词）才丢给裁判判同义 —— 这是 C/E 组唯一用到裁判的地方。

**`exist_negative`**：有/无判定 + **yes 偏置率**（模型有多倾向于说「有」）。我们的负样本是易混类别的 hard negative，相当于 POPE 的 Adversarial 档 —— **不要拿 POPE 的公开数字横向比**。

**验收**：上位判定要有单测覆盖「金标是子类、答案是父类」「反过来」「毫不相干」三种。

### 阶段 3 — 格式合规 / 串味 / 截断（§9）

微调模型特有的失败模式，公开评测集不覆盖：

- **坐标格式合规率**：能否解析成 `bbox_2d`、是否越界、是否自作主张加围栏（`bbox.ParsedBoxes.flags` 已经在记，汇总即可）
- **任务串味率**：问框答文字、问描述吐坐标
- **截断率**：撞上 `max_new_tokens` 的比例

> **对比 base 时的陷阱**：Qwen3-VL base 会自己加 ` ```json ` 围栏。解析器不宽容的话，base 的 Acc 会因**格式**而虚低，报表会好看得离谱 —— 那测的是格式差异不是能力差异。**围栏只计数不扣分**，`bbox.py` 已经这么做了，不要改。

### 阶段 4 — profile 机制（§13）

> **profile 只回答一个问题：这种 kind 的数据，用哪个打分器、哪版提示词。**

数据的 `kind` 从数据集配置来。换一套数据不用改 profile，换一套口径不用改数据 —— 评通用问答时切 `general_vqa` profile 全走裁判，完全不受这套确定性打分器影响。

```json
"profile": {
  "name": "grounding_zh_v1",
  "scorers": {
    "grounding_single": { "engine": "code", "impl": "bbox_deviation",
                          "params": { "iou_gate": 0.5, "dev_thresholds": [20, 50] } },
    "object_ident":     { "engine": "code", "impl": "class_name_match",
                          "params": { "hypernym_table": "classes.yaml" },
                          "fallback": { "engine": "judge", "prompt": "judge_synonym_v1.txt" } },
    "describe":         { "engine": "judge", "prompt": "judge_describe_v1.txt",
                          "dims": ["correct", "grounded", "informative"],
                          "code_precheck": "describe_scope_v1" }
  },
  "pairwise": ["describe"],
  "history_modes": ["gold", "model"],
  "model_history_sample_ratio": 0.3
}
```

**只有 D 组做 pairwise。** A/B/C/E 组有确定答案，pointwise 的绝对分就够了，pairwise 纯属浪费裁判调用。

### 阶段 5 — `history_mode: model` + 链路衰减率（§7）

现状：`convert_vqa_json.py` 的文档字符串写明历史轮**回放标准答案**。也就是评描述时喂给模型的是**正确的框** —— 但真实部署时框是模型自己出的，框错了描述必然描述错东西。

**三次前向，不是四次**：

```
第 1 轮：问「框出 X」          → B_pred        ← A 组指标在这里算
第 2 轮 gold ：历史塞 B_gt      → D_gold
第 2 轮 model：历史塞 B_pred    → D_model
```

第 1 轮只跑一次，两种历史共用。`D_model` 只对 **30% 抽样子集**跑 —— 链路衰减率是统计量，不需要全量。

```
链路衰减率 = (D_gold 得分 − D_model 得分) / D_gold 得分
```

| 现象 | 结论 |
|---|---|
| 衰减大 | 瓶颈在**定位** —— 框错了描述再好也白搭，补定位数据 |
| 衰减小但 `D_gold` 本身低 | 瓶颈在**描述** —— 补描述数据 |
| 两个都高 | 这一轮 SFT 成了 |

### 阶段 6 — D 组裁判 + 代码前置检查（§8）

四个维度，**分工写死**：

| 维度 | 引擎 | 说明 |
|---|---|---|
| 正确性 | 裁判 | 说的内容图里有吗（幻觉） |
| 落地性 | 裁判 | 说的是不是**这个框里**的东西 |
| 信息量 | 裁判 | 是不是「一辆车」这种空话 |
| **范围合规** | **代码** | `appearance` 有没有跑去说方位、`position` 有没有跑去说外观 |

**范围合规必须走代码**，复用数据集项目 `prompts/describe/*.txt` 里的 `#! must-not:` 词表。理由：通用的「描述准确性」rubric 会给跑题答案高分（说得没错啊），**裁判判不出「跑题」这件事**。

7 种 describe kind 的答案信息结构完全不同，**报表必须有 7 行**，不许合成一个平均分。

**可直接搬的现成代码**（在数据集项目 `qwen3vl_sft_builder/core/stats.py`）：
- `chair()` —— 算「文本里提到的类别名有多少不在图里」，喂模型输出就是模型的幻觉率
- `pope_balance()` —— 就是 yes 偏置

### 阶段 7 — 配对 bootstrap + 连续量汇总（§15.2）

`eval_tool/aggregate.py:10` 的 `bootstrap_binary_ci` 有两个问题：

1. **非配对**。两个模型跑在**同一批样本**上，配对重采样的置信区间会窄得多，更容易看出小幅提升是不是真的。→ 新增 `bootstrap_paired_diff_ci()`，对同一样本的差值重采样。
2. **只接受 0/1**（`summarize_binary_metric` 的 `metric_col="hit"`）。四点偏差和 IoU 是连续值。→ 新增连续量版本。

### 阶段 8 — 报表拆分 + 标灰规则（§16、§11.4）

五个维度，每格带 `n` 和 CI：

| 维度 | 行数 | 适用组 |
|---|---|---|
| `meta_task_type` | 14 | 全部 |
| `meta_difficulty` | 3 | **只在组级别汇总** |
| `meta_size_bucket` | 3 | A / B 组 |
| `meta_label` | 最差 20 | A / B / E 组 |
| `meta_describe_kind` | 7 | 仅 D 组 |

**标灰规则**（统计功效，§11.4）：

| n | 最坏 95% CI 半宽 | 结论强度 |
|---|---|---|
| 260 | ±6.1pp | 可下结论 |
| 100 | ±9.8pp | 可下结论 |
| 50 | ±13.9pp | **只看趋势，标灰** |
| < 30 | — | 显示 `n=xx（样本不足）`，**不显示百分比** |

> **【设计上为空的格子，不是 bug】**
> `ground_part` 和 `ground_contrast` 的提示词带 `#! max-grade: medium`，**设计上就不会有 hard 档样本**（困难目标看不清部件、也分不出和同类的差别，硬写就是编）。
> 这两格的 hard 列要标注为**「设计上为空」**，和「样本不足」**区分显示**。看到空格当 bug 修就错了。

### 阶段 9 — 双向一致性 + 问法扰动（§10）

**双向一致性**（借 Ferret 的 Referring ↔ Grounding 对偶）：

```
正向：问「框出白色面包车」     → 框 B
反向：拿 B 问「这个框里是什么」 → 类别 X'
                   X' 和「白色面包车」对得上吗？
```

**不需要任何额外标注**。判定复用阶段 2 的上位三档。这是整套评估里**信息量最高的单个指标** —— 它测的是模型有没有真的建立文字 ↔ 区域的双向映射，还是只单向背下了「看到『框出』就吐坐标」。

**问法扰动鲁棒性**：同一目标用 3 种不同说法问，答案框应该一样。报三次输出之间的**四点偏差方差**和 **IoU 一致率**（两两 IoU ≥ 0.9 视为一致）。抽 300 条 A 组样本做。

测的是对问法的过拟合 —— SFT 数据的问法有限，模型很容易学成「看到某个固定句式才输出坐标」，换个说法就崩。标准评测集测不出来，因为它们的问法也是固定的。

---

## 5. 裁判可信度（§14）—— 影响 D 组结论怎么写

**不做人工锚点。** 人工标 100~200 条的锚本身可能偏，成本高且不可靠，反而会给出一个虚假的「绝对正确率」。

**已知局限，每份报告都要写**：裁判是 Qwen3.8-27B，被测是 Qwen3-VL-8B，**同家族**，裁判池目前只有 Qwen，**无法做自偏检测**。这一条不许省略。

替代方案三重交叉验证：

| 手段 | 读法 |
|---|---|
| rubric 版本间一致性（同一批 300 条两版 rubric，报 Spearman + 平均绝对差） | 一致率低 → rubric 有歧义，**先改提示词再看分数**，否则是在解读噪声 |
| 位置互换一致性（pairwise 正反各判一次，现有代码已实现） | 正反结论不一致的比例即位置偏置 |
| **代码指标交叉验证**（D 组的范围合规率、CHAIR 幻觉率、空话率与裁判分数**并列报**） | 两者走向不一致 → **裁判有问题**，以代码指标为准 |

第三条最重要：这三个代码指标**完全不受裁判偏置影响**，是 D 组唯一的客观锚。

**D 组分数只能做纵向对比**（base vs sft，同一套裁判、同一版 rubric、同一个 `eval_set_sha`），**不能当绝对值对外报**。

---

## 6. 评估集与冻结（§11、§12）

- 评估集从**那 8 万张**的产出里切（当前 14,726 张只产出 19,655 条，test 仅约 983 条，不够 §11 要求的 2000 条）
- **主分层维度只有 `task_type`**。难度档、尺寸档、类别、describe kind 都是**事后拆分**，不参与抽样保量 —— 2000 条 ÷ 42 格每格不到 50 条，全得标灰，交叉分层等于白做
- 评估集**难度分布不必等于训练集**：训练集 hard 占 10%，评估集提到每任务内 ≥ 25%，**前提是三档分开报、不做跨档加权汇总**
- **抽样一次后固化为 `eval_set_v1.jsonl`，记 `sha256`**

每份报告必须记四个指纹，**任一不同的结果不许画进同一张对比图**（报表层硬校验）：

```
eval_set_sha / profile_version / rubric_version / judge_model
```

**正确顺序：数据全部建完 → 切 test → 冻结 → 才开始评模型。** 别现在切了以后又加数据重切，那样前后 checkpoint 的成绩没法比。

---

## 7. 预算（§18）

每个 checkpoint：

| 推理 | 次数 | | 裁判 | 次数 |
|---|---:|---|---|---:|
| 第 1 轮（2000 条） | 2000 | | D 组 pointwise | 1482 |
| D 组 gold 第 2 轮 | 1140 | | D 组 pairwise（500×互换） | 1000 |
| D 组 model 第 2 轮（30%） | 342 | | rubric 一致性（300 条） | 300 |
| 双向一致性反向问 | 1140 | | C/E 同义兜底（≈5%） | 15 |
| 问法扰动（300×2） | 600 | | | |
| **合计** | **≈ 5222** | | **合计** | **≈ 2800** |

checkpoint 数量待定，成本线性叠加。正因为不定，§12 的评估集冻结是**强制项**。

---

## 8. 还缺的输入

| # | 缺什么 | 卡住哪个阶段 |
|---|---|---|
| 1 | **`classes.yaml`（248 类那份）** —— 不在任何 git 仓库里，在服务器上 | 阶段 2（上位判定）、阶段 6（CHAIR 词表）、阶段 9（双向一致性判定） |
| 2 | **一份真实 `test.jsonl` 样本**（20 条即可） | §3.3 的 convert，按真实字段写而不是照猜 |
| 3 | 那 8 万张跑完的产出 | §6 切评估集 |

阶段 3、7 不依赖以上任何一项，**可以先做**。

---

## 9. 提交与把关

- 分支 `claude/model-eval-grounding`，**不要直接推 main**
- 每个阶段一个提交，提交信息写清**做了什么 + 为什么这么定**（不是「按文档实现了阶段 X」）
- 每个提交必须：`python -m pytest tests/ -q` 全绿，且 `git diff --stat -- tests/` 对**已有测试文件**为空
- 新增打分器必须覆盖需求正本 §3.5 的全部退化情形（`tests/test_bbox.py` 是模板，照它的粒度写）

**把关方（Claude）会逐条对着需求正本审**，重点看这几件：

1. `engine: code` 的打分器有没有偷偷调模型
2. 偏差有没有在定位失败的样本上算（那会把整体拉成噪声）
3. `Acc@dev<=N` 是不是取的最差角而不是平均
4. 有没有为了省事把「定位成功率」和「偏差」合成一个数
5. 有没有装 `pycocotools` 去算 AP
6. 报表有没有把「设计上为空」和「样本不足」混为一谈
7. 有没有改动已有测试的断言
