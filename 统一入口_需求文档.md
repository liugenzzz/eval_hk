# 统一推理/评估入口 — 需求文档

状态：需求已澄清，待确认后进入设计与实施
日期：2026-07-31

---

## 一、背景与问题

当前有 5 个入口、2 套互不相通的配置 schema：

```
python -m eval_tool.convert_vqa_json <json> <dir>        # 数据转换（位置参数，无配置）
python -m eval_tool.run_infer --config infer_config.json # InferConfig
python -m eval_tool --config config.json                 # EvalConfig
python run_rubric.py --config config.json --rubric v4    # 派生 json + subprocess 调上一条
python compare_rubrics.py v4=path v4b=path               # 事后分析，手工拼 LABEL=PATH
```

### 已确认的四个问题

**P1 — 配置字段跨文件重复，改一处漏一处不报错**
`tsv_dir`、`datasets` 在 `infer_config.json` 和 `config.json` 里各写一遍。不一致时不会报错，只会算错。

**P2 — 推理产物路径靠手工抄**
`run_infer` 按 `prediction_output_path(out_dir, model_name, dataset_name)` 写出
`<out_dir>/<model>_<dataset>.xlsx`（infer.py:133），你要把这个路径手工填进 eval config 的
`models[].vqa`。抄错的后果是拿旧预测跑新评估，全程静默。

**P3 — 推理侧不支持多模型，评估侧支持**
`InferConfig` 只有单个 `model_name`/`model_path`（config.py:55），`EvalConfig` 有 `models` 列表。
跑 base + sft 对比要改三次配置跑三遍。这是两侧最大的结构落差。

**P4 — 推理中断即全丢（本轮新增的核心需求）**
`run_infer.run()` 的流程是：`generate_predictions()` 把全部结果攒在内存 list → 循环结束后
`output.to_excel(out_path)` 一次性写盘（run_infer.py:56-66）。**过程中没有任何持久化**。
对比裁判侧：`JsonlCache.set()` 每判一条立即 append 落盘（cache.py:40-45），重跑天然续传。
两侧可靠性完全不对等。

`overwrite` 标志的粒度是整个文件（run_infer.py:37）：要么整个数据集已跑完直接跳过，
要么从头再来。没有"跑了 800/1000 条"这个状态。

**P5 — 多卡并行下的静默数据污染（附带发现，非用户提出）**
`merge_indexed_predictions` 用空串 `""` 填充未返回的位置（infer.py:192-197）。
任一 worker 进程异常退出 → 该分片全部预测变成空字符串 → 裁判判为"答非所问"给低分
→ 报表显示为模型能力退步。**不抛异常、不进 warnings.log，无法从结果反推。**
`_run_dataset_parallel` 用 `as_completed` 收集全部 future 后才合并，单个 worker 崩溃
同样丢失其余 worker 的全部已完成工作。

---

## 二、已确认的需求

| # | 需求 | 来源 |
|---|------|------|
| R1 | 单配置文件 + 子命令，推理产物路径由代码推导并自动接给评估 | 已确认 |
| R2 | 支持一条命令跑完（convert→infer→eval），**同时**每个子命令必须能独立运行 | 已确认 |
| R3 | **推理支持断点续传**：中断后重跑只补未完成的条目 | 已确认 |
| R4 | **同一份推理结果可被反复复用**：改裁判提示词只重跑评估，不碰推理 | 已确认 |
| R5 | 推理侧改用 `models` 列表，一次跑多个模型 | 已确认 |
| R6 | rubric 版本切换并入统一入口（替代 run_rubric.py 的 subprocess 方案） | 已确认 |
| R7 | 支持一次跑多个 rubric 并自动出 compare 对比表 | 已确认 |
| R8 | `convert_vqa_json` 并入为子命令，共享 tsv_dir 配置 | 已确认 |
| R9 | 旧命令行与旧配置文件继续可用（向后兼容） | 已确认 |
| R10 | 修复 P5 的静默空预测污染 | 建议纳入，待确认 |

### R4 的现状说明（这条大部分已经满足）

"改提示词复用推理结果"在**裁判层已经实现**：`judge_fp` 是裁判模型+温度+两份提示词全文的
哈希，换 rubric 会自然缓存失配并重判（run_eval.py:33-36），推理产物完全不受影响。
`models[].scored` 字段还能让 baseline 连裁判 API 都不用重调（config.py:17-22）。

**所以 R4 的缺口只在易用性**：目前要手工维护 `models[].vqa` 指向的 xlsx 路径。
R1 的自动接线解决这一条，不需要新机制。

---

## 三、目标设计

### 3.1 命令行

```bash
python -m eval_tool convert <sharegpt.json>          # 写 tsv_dir/aero_vqa.tsv
python -m eval_tool infer   --config pipeline.json   # 默认续传
python -m eval_tool eval    --config pipeline.json --rubric v4
python -m eval_tool sweep   --config pipeline.json --rubrics v1,v3,v4,v4b
python -m eval_tool all     --config pipeline.json   # convert→infer→eval 串起来
```

所有子命令共用一份 `pipeline.json`。`--models base,sft_ep2` 可对任意子命令做子集过滤。

### 3.2 配置结构

```json
{
  "tsv_dir": "/code/LMUData",
  "datasets": { "vqa": "aero_vqa" },
  "work_dir": "/code/eval_model_output",
  "out_dir": "eval_report",
  "cache_dir": "eval_cache",

  "models": [
    { "name": "base",    "model_path": "/code/.../Qwen3-VL-8B-Instruct" },
    { "name": "sft_ep2", "model_path": "/code/.../sft_ep2" }
  ],
  "baseline_model": "base",

  "infer": {
    "prompt_files": { "vqa": "prompts/infer_vqa_raw.txt" },
    "max_new_tokens": 1024,
    "batch_size": 1,
    "gpu_ids": [],
    "workers_per_gpu": 1
  },

  "judge": {
    "api_base": "http://192.168.48.7:18180/v1/chat/completions",
    "model": "qwen3.6-27b",
    "prompt_files": { "pointwise": "...", "pairwise": "..." }
  }
}
```

**模型定义只写一次**，`model_path` 给推理用、`name` 给评估用。
预测文件路径按约定推导：`work_dir/<name>/<name>_<dataset_name>.xlsx`。

三种覆写口径（优先级由高到低）：
- `models[].scored.<ds>` — 已评分明细，跳过预测读取和裁判调用
- `models[].pred.<ds>` — 外部生成的预测文件，跳过推理
- 不写 — 走约定路径，由 infer 子命令产出

### 3.3 断点续传（R3，核心新增）

**机制**：推理逐条写 JSONL 分片文件，与裁判缓存同构。

```
work_dir/<model>/_partial/<dataset>__<infer_fp>[__w<k>].jsonl
每行: {"index": "...", "prediction": "..."}
```

- `infer_fp` = `model_path + 推理提示词全文 + max_new_tokens + dataset_key` 的哈希。
  改了推理提示词 → 指纹变化 → 旧分片不被复用（与 `judge_fp` 同一思路）
- 每个 batch 结束 flush 一次，不等整轮结束
- 启动时载入分片，已有 index 直接跳过；日志打印 `[infer] 续传：已完成 812/1000`
- 并行模式下每个 worker 写自己的 `w<k>` 分片，避免多进程争用同一文件
- 整轮完成后汇总为最终 xlsx；分片文件保留（见待确认 Q1）

**同时修复 P5**：合并时若仍有 index 缺失，写入 `warnings.log` 并**拒绝生成 xlsx**
（或生成但标记为 partial），不再用空串静默填充。

### 3.4 rubric 管理（R6/R7）

`--rubric v4` 直接在内存构造 config，不再派生 json 文件 + subprocess。
保留 run_rubric.py 现有的两个防呆：跑前打印 `judge.fingerprint`、out_dir 自动带版本后缀。

`sweep` 子命令按 `--rubrics` 依次跑，产出目录为 `<out_dir>_<rubric>/`，
全部完成后自动以正确的 `LABEL=PATH` 调用 compare_rubrics 出表。

### 3.5 向后兼容（R9）

`run_infer.main()` / `run_eval.main()` 保留为薄封装，旧配置格式由 `load_infer_config` /
`load_config` 继续解析。现有 nohup 命令、文档、脚本不用改。
新旧配置在同一份 `_load_raw_config` 上分流，靠是否存在顶层 `infer` + `models[].model_path` 区分。

---

## 四、影响面

| 文件 | 改动 |
|------|------|
| `eval_tool/cli.py` | 新增，argparse 子命令分发 |
| `eval_tool/config.py` | 新增 `PipelineConfig` + 从它派生 InferConfig/EvalConfig；旧两个 loader 保留 |
| `eval_tool/infer_cache.py` | 新增，推理分片持久化（可复用 JsonlCache） |
| `eval_tool/run_infer.py` | 多模型循环、续传接入、并行分片落盘、P5 缺失校验 |
| `eval_tool/run_eval.py` | 接受已构造的 config 对象（当前只接受路径） |
| `eval_tool/__main__.py` | 转发到 cli.py，保留无子命令时按旧行为跑 eval |
| `run_rubric.py` | 降级为兼容 shim |
| `compare_rubrics.py` | 抽出可被 import 的函数供 sweep 调用 |
| `tests/` | 新增续传、多模型、配置派生、缺失预测校验的测试 |
| 文档 | README + 开放问答评估_使用说明.md 更新 |

不改动：`judge.py`、`judge_rubrics.py`、`score_vqa.py`、`aggregate.py`、`report.py`、
`prompts/*`、`gen_rubric_prompts.py`。评分口径完全不动。

---

## 五、待确认（Q1-Q5）

| # | 问题 | 我的建议默认值 |
|---|------|--------------|
| Q1 | 推理分片 JSONL 跑完后保留还是删除？ | **保留**。占空间但能事后核查；给 `--clean-partial` 手动清 |
| Q2 | 推理指纹变化（改了推理提示词）时的行为？ | **报错并提示**，要求显式加 `--overwrite`。静默全量重跑一次要几小时 GPU |
| Q3 | 续传默认开还是要显式 `--resume`？ | **默认开**，`--overwrite` 显式从头跑 |
| Q4 | sweep 多个 rubric 是串行还是并行调裁判 API？ | **串行**，避免打爆本地 27B 推理服务 |
| Q5 | P5（空串填充）纳入本次范围？ | **纳入**，它和续传改的是同一段合并逻辑 |

---

## 六、交付顺序建议

1. **先做 R3 续传 + P5 修复**（不依赖配置重构，独立可上线，止血价值最高）
2. 再做 R1/R5 配置统一 + R9 兼容层
3. 最后做 R6/R7 rubric 子命令与 sweep
4. 文档同步

第 1 步单独交付的理由：你现在正要跑 4 组 rubric 对比，每组都要完整推理一遍；
推理不能续传的情况下，任何一次中断都是几小时 GPU 白烧。
