# 边训边评（LLaMA-Factory × eval_tool × VLMEvalKit）— 设计文档

状态：决策已拍板，可据此开工
日期：2026-09-08
上游文档：`边训边评_需求文档.md`（需求梳理阶段，其中的待确认项已由本文档结论取代）

---

## 一、系统结构

```
┌──────────────────────────┐
│ LLaMA-Factory (训练机/集群) │
│  TrainerCallback.on_save  │
└────────────┬─────────────┘
             │ 写信号文件（共享盘）
             ▼
   <signal_dir>/<run_id>/step-<step>.json
   <signal_dir>/<run_id>/step-<step>.json.done
             │
             ▼
┌──────────────────────────────────────────────────────┐
│ 编排器 orchestrator（评估机，常驻，本项目新增）            │
│  扫描 → 校验 → 去重 → 节流 → 分级队列 → GPU 独占锁       │
└───────┬──────────────────────────────┬───────────────┘
        │ 领域线                        │ 通用线
        ▼                              ▼
  python -m eval_tool               torchrun run.py
  (本仓库，LLM 裁判)                 (vendor/VLMEvalKit)
        │                              │
        │ score_summary.csv            │ status.json
        │ judge_detail_all.xlsx        │ *_acc.csv
        └──────────────┬───────────────┘
                       ▼
              归一化 adapter → 统一 result schema
                       ▼
   eval_result.json / eval_trend.csv / 最终 HTML 报告 / 数据配比建议
```

**三个进程各自独立**：编排器只用文件和子进程与两条评估线通信，不 import 对方代码。
理由：两边依赖栈不同（VLMEvalKit 需要 torchrun/decord/HF 数据集），启动方式不同，
裁判服务不同；进程隔离后任何一边崩溃、OOM、卡死都不影响另一边和编排器本身。

**评估永不影响训练**：编排器不向训练侧回传任何中断/早停信号，只产出报告与建议。
训练崩没崩由人看报告决定，不做自动早停。

---

## 二、接口契约 A：训练侧 → 评估侧

### A-1 信号目录与写法

**信号目录是固定的，checkpoint 路径是动态的。** 这两件事必须分开：

```
信号目录（固定，双方约定死，评估侧就扫这一个地方）
  /mnt/si003010kcx0/mmdata/mmcode/trainspace/eval_signals/

checkpoint 路径（每次训练都不同，由信号内容携带）
  信号里的 checkpoint_path 字段
```

信号目录不能跟着 `output` 走——评估侧的常驻进程需要一个固定的地方去扫，
路径每次训练都变的话，编排器就不知道该看哪里。动态的部分放进 JSON 内容里传。

写入路径：

```
<signal_dir>/<run_id>/step-<step>.json        # 先写
<signal_dir>/<run_id>/step-<step>.json.done   # 后写，空文件
<signal_dir>/<run_id>/finished.json           # 训练结束
<signal_dir>/<run_id>/finished.json.done
```

顺序是硬要求：**checkpoint 目录完全写完 → 原子写 .json → fsync → 写 .done**。
评估侧只扫 `*.done`，看到才读同名 `.json`。不允许用目录 mtime、文件大小或
`checkpoint-*` 目录是否出现来判断保存完成——LLaMA-Factory 保存是多文件写入，
读到写了一半的目录会加载出错误权重且不报错。

### A-2 `run_id` 生成规则

```
<模型简称>_<数据版本>_<YYYYMMDD>_<HHMM>

例：qwen3vl8b_zbv3_20260908_1430
正则：^[a-z0-9][a-z0-9_]{2,63}$
```

三条约束：

1. **一次训练内固定不变**，中途不能改
2. **断点续训沿用同一个 `run_id`**（同一次训练的延续，趋势要连成一条线）
3. **不同训练不能重复**（日期时间后缀负责这一点）

`run_id` 是趋势表的分组主键，重复会把两次训练的曲线混成一条。

### A-3 `checkpoint_saved` 事件

**下表就是全部字段，全部必填，没有可选项。** 多余的字段我会忽略，缺任何一个直接判 `failed`。

```json
{
  "schema_version": "1.0",
  "event": "checkpoint_saved",
  "request_id": "qwen3vl8b_zbv3_20260908_1430__step600",
  "run_id": "qwen3vl8b_zbv3_20260908_1430",
  "step": 600,
  "epoch": 1.5,
  "saved_at": "2026-09-08T12:00:00+08:00",
  "checkpoint_path": "/mnt/si003010kcx0/mmdata/mmcode/trainspace/output/zb_v3/checkpoint-600",
  "base_model_path": "/mnt/si003010kcx0/mmdata/mmcode/trainspace/model/Qwen3-VL-8B-Instruct",
  "is_final": false,
  "train_meta": {
    "stage": "sft",
    "finetuning_type": "full",
    "mix_strategy": "concat",
    "datasets": [
      { "name": "zb_equip", "samples": 12000 },
      { "name": "books", "samples": 3000 },
      { "name": "general_mix", "samples": 4500 }
    ]
  }
}
```

| 字段 | 类型 | 评估侧拿它做什么 |
|------|------|------------------|
| `schema_version` | string | 固定 `"1.0"`。不匹配直接拒收，避免协议悄悄漂移 |
| `event` | string | 固定 `"checkpoint_saved"`，区分两类信号 |
| `request_id` | string | 幂等键，格式 `<run_id>__step<step>`。重复投递跳过 |
| `run_id` | string | 趋势表分组主键，见 A-2 |
| `step` | int | 趋势横轴 |
| `epoch` | float | 报告第二横轴、崩点定位时对应到"训到第几轮" |
| `saved_at` | string | RFC3339 带时区。用于孤儿信号超时判断，不用于排序 |
| `checkpoint_path` | string | 绝对路径，两条评估线的 `--model-path` 直接指过去 |
| `base_model_path` | string | base 对照组，所有 `delta_vs_base` 的分母 |
| `is_final` | bool | `true` 时无视节流必评，且直接进 L2 |
| `train_meta.stage` | string | `"sft"` / `"dpo"` / `"pt"`，报告归档 |
| `train_meta.finetuning_type` | string | `"full"` / `"lora"`。与 checkpoint 实际形态交叉校验，不一致直接判 `failed` |
| `train_meta.mix_strategy` | string | **必须是 `"concat"`**，见 A-4 |
| `train_meta.datasets[].name` | string | 数据集名，配比建议按它分行 |
| `train_meta.datasets[].samples` | int | **实际参训条数**，配比建议的基准，见 A-4 |

已从早期草案中删除的字段：`total_samples`（可由 `datasets[]` 求和得出，两处不一致时无从判断信谁）、
`learning_rate` / `num_train_epochs`（评估侧不使用，超参存档走训练侧自己的 config，不占用本协议）。

### A-4 `train_meta.datasets[].samples` 的口径

配比旋钮定为**各数据集实际条数**，所以这个数必须是"这次训练实际喂进去多少条"，
不是数据文件里有多少条。两种情况：

| 训练配置 | `samples` 填什么 |
|---|---|
| 设了 `max_samples: N` | `min(N, 该数据集文件实际行数)` |
| 没设 `max_samples`（全量） | 该数据集文件实际行数 |

**`mix_strategy` 必须是 `concat`（LLaMA-Factory 默认值）。**

用 `interleave_under` / `interleave_over` 的话，各数据集实际被采样的条数由
`interleave_probs` 和总步数共同决定，"实际参训条数"这个概念不成立，
我输出的 `books: 3000 → 4000` 也就无法落地。

如果训练侧确实必须用 interleave，现在告诉我：配比建议的单位改成 `interleave_probs`，
信号里 `datasets[].samples` 换成 `datasets[].prob`，规则表同步改。**二选一，不能两种都支持**——
两种单位混在一张趋势表里，历史数据会失去可比性。

### A-5 `run_finished` 事件

同样全部必填。

```json
{
  "schema_version": "1.0",
  "event": "run_finished",
  "request_id": "qwen3vl8b_zbv3_20260908_1430__finished",
  "run_id": "qwen3vl8b_zbv3_20260908_1430",
  "status": "completed",
  "final_step": 2000,
  "final_checkpoint_path": "/mnt/si003010kcx0/mmdata/mmcode/trainspace/output/zb_v3/checkpoint-2000",
  "finished_at": "2026-09-08T20:31:00+08:00"
}
```

| 字段 | 说明 |
|---|---|
| `status` | `completed` / `failed` / `interrupted`，三选一 |
| `final_step` | 最后一个 step |
| `final_checkpoint_path` | 最终权重，L3 终评的对象 |
| `finished_at` | RFC3339 带时区 |

`failed` / `interrupted` 时评估侧仍出报告（用已积累的趋势数据），只是不跑 L3。

---

## 三、接口契约 B：评估侧输出

### B-1 单次结果 `eval_result.json`

写到 `<report_root>/<run_id>/step-<step>/eval_result.json`。

```json
{
  "schema_version": "1.0",
  "request_id": "qwen3vl8b_zb_sft_v3__step600",
  "run_id": "qwen3vl8b_zb_sft_v3",
  "step": 600,
  "epoch": 1.5,
  "level": "L1",
  "status": "succeeded",
  "model_name": "qwen3vl8b_zb_sft_v3_step600",
  "suites": [
    {
      "suite": "equip_desc",
      "display_name": "装备描述开放问答",
      "line": "domain",
      "primary_metric": "score",
      "n": 300,
      "sampled": true,
      "metrics": [
        { "key": "score",    "metric_type": "llm_pointwise", "value": 0.7213,
          "n": 300, "ci95": [0.6802, 0.7594], "delta_vs_base": 0.0841 },
        { "key": "score_单轮", "metric_type": "llm_pointwise", "value": 0.7502, "n": 190 },
        { "key": "score_多轮", "metric_type": "llm_pointwise", "value": 0.6810, "n": 110 }
      ],
      "artifacts": { "report_dir": "...", "detail_xlsx": "..." },
      "fingerprint": { "judge_fp": "…", "tsv_sha256": "…", "rubric": "v4",
                       "sample_file": "samples/equip_desc__n300__seed42.json" }
    },
    {
      "suite": "general_vlm",
      "display_name": "通用回归",
      "line": "general",
      "primary_metric": "mean_accuracy",
      "n": 1565,
      "sampled": true,
      "metrics": [
        { "key": "mean_accuracy",  "metric_type": "accuracy", "value": 0.6712, "n": 1565,
          "delta_vs_base": -0.0210 },
        { "key": "RealWorldQA",    "metric_type": "accuracy", "value": 0.7020, "n": 765 },
        { "key": "MMMU_DEV_VAL",   "metric_type": "accuracy", "value": 0.5875, "n": 400 },
        { "key": "MathVista_MINI", "metric_type": "accuracy", "value": 0.7150, "n": 400 }
      ],
      "artifacts": { "work_dir": "...", "status_json": "..." },
      "fingerprint": { "judge_model": "fx-q3-235", "vlmevalkit_commit": "c8951f0" }
    }
  ],
  "alerts": [
    { "kind": "general_regression", "suite": "general_vlm",
      "message": "通用回归相对 base 下降 2.1pt", "severity": "warn" }
  ],
  "timing": { "queued_at": "…", "started_at": "…", "finished_at": "…",
              "domain_seconds": 380, "general_seconds": 1120 },
  "error": null
}
```

**`metric_type` 挂在每个指标上，不挂在 suite 上。**
军事异常检测一个 suite 内就同时有 `map` / `accuracy` / `llm_pointwise` 三种量纲，
挂在 suite 上会在检测领域接入时导致报告层重写。

`status`：`succeeded` / `partial`（部分 suite 失败）/ `failed` / `skipped`（重复或被节流丢弃）。

### B-2 趋势表 `eval_trend.csv`

`<report_root>/<run_id>/eval_trend.csv`，每次评估追加。**长格式**，一行一个 (step, suite, metric)：

```
run_id, step, epoch, level, status, suite, line, metric_key, metric_type,
value, n, sampled, ci95_low, ci95_high, delta_vs_base, is_primary, finished_at, fingerprint
```

用长格式而不是宽格式：检测领域接入后指标列会不断增加，宽格式每加一个领域就要改表结构。

### B-3 最终报告

`<report_root>/<run_id>/report.html` —— 一个自包含 HTML，给算法/领导/归档三方看：

- 顶部：本次训练结论摘要（best checkpoint、各领域最终分、是否触发回归告警）
- 各领域趋势线（step 横轴，一个领域一条线，标出 base 水平线）
- 通用 6 个 benchmark 相对 base 的对比（L3 终评数据）
- 崩点定位（见 §7）
- 下一轮数据配比建议表（见 §8）
- 附录：全部 fingerprint、抽样文件、各阶段耗时

---

## 四、Suite 配置（领域可扩展的核心）

一个领域一份 `suites/<suite_id>.json`。suite id 一旦发布**不可更名**——它进目录名、
进趋势表主键、进指纹，改名等于历史数据作废。

### 4.1 已定名的 suite

| suite id | 中文名 | line | scorer | 状态 |
|---|---|---|---|---|
| `equip_desc` | 装备描述开放问答 | domain | `llm_pointwise` | 现有 |
| `books` | 书籍领域 | domain | `llm_pointwise` | 现有 |
| `journal` | 期刊领域 | domain | `llm_pointwise`（`extends: books`） | 复用书籍配置，只换 tsv |
| `device_anomaly` | 设备异常识别 | domain | 待定 | 占位，待开发 |
| `military_anomaly` | 军事异常检测 | domain | 多指标，见下 | 占位，待开发 |
| `aircraft_part_anomaly` | 飞机螺丝及结构件异常检测 | domain | 多指标，见下 | 占位，待开发 |
| `general_vlm` | 通用回归（VLMEvalKit） | general | `vlmevalkit` | 现有 |

检测类 suite 的指标结构（现在占位，逻辑待开发时填）：

```
military_anomaly
├── localization       metric_type: map           异常检测定位
├── category_accuracy  metric_type: accuracy      异常类别名称
└── desc_consistency   metric_type: llm_pointwise 异常描述语义一致性评分   ← primary

aircraft_part_anomaly
├── defect_accuracy    metric_type: accuracy      功能缺陷识别准确率        ← primary
└── category_accuracy  metric_type: accuracy      异常类别识别准确率

device_anomaly
└── （指标口径待定，先占 suite id）
```

### 4.2 配置结构

```json
{
  "suite_id": "journal",
  "display_name": "期刊领域",
  "line": "domain",
  "extends": "books",
  "scorer": "llm_pointwise",
  "primary_metric": "score",
  "tsv_dir": "/shared/LMUData/journal",
  "datasets": { "vqa": "journal_vqa" },
  "sample": { "n": 300, "seed": 42, "stratify_by": ["category"] },
  "levels": { "L1": true, "L2": true, "L3": true }
}
```

`extends` 只继承 prompt、裁判配置、rubric、评分参数，**不继承数据路径**。

### 4.3 扩展代价（说清楚边界）

- **同类型领域**（LLM 裁判类）新增 → 加一个 json，**零代码**。期刊复用书籍即是此例。
- **新指标类型**（检测类）新增 → 需要在 `scorers/` 下加一个实现（mAP 怎么算、IoU 阈值、
  类别名同义词匹配），并在 suite json 里声明 `scorer`。

  但**编排器、信号协议、队列、节流、趋势表、报告层、HTML 全部不动**。
  代价被限制在一个新文件 + 一行配置。

---

## 五、调度：分级、节流、GPU 独占

### 5.1 分级

| 级别 | 内容 | 触发 | 队列策略 | 目标耗时 |
|---|---|---|---|---|
| **L1** | 各领域集抽样 + 通用轻量三件套 | 每个 checkpoint（受节流） | 只保留最新一个 | 20-30 分钟 |
| **L2** | MMMU 全量 + MathVision + ERQA | `is_final` 或人工触发 | 只保留最新，旧的标 `superseded` | 小时级 |
| **L3** | Video-MME + 全部全量 | `run_finished` 之后一次 | 单次 | 十小时级 |

正在跑的任务**不抢占、不杀**，跑完当前的再取队首；只清理排队中的旧任务。
理由：中途杀掉会让 VLMEvalKit 的 `status.json` 停在 `infer` 态，
"这个 checkpoint 到底评完没有"的判定会变复杂，`--reuse` 续跑的收益不抵这个复杂度。

### 5.2 节流（针对存盘节奏不可预测）

训练存盘节奏是动态的——可能一个 epoch，可能 500 步，也可能 20 步。20 步一存时
L1 自身也会堆积。规则：

- **同一 `run_id` 内两次 L1 至少间隔 30 分钟**（可配 `min_interval_minutes`）
- 间隔期内到达的 checkpoint 全部入"待评槽"，**只保留最新的一个**，被顶掉的记 `skipped`
- **`is_final = true` 和 `run_finished` 无视节流，必评**

被 `skipped` 的 checkpoint 仍写一条 `status: skipped` 的结果记录，趋势表里可见断点，
不会让人误以为那段时间没训练。

### 5.3 GPU 独占

8×80G，**全局一把锁，同一时刻只有一个评估 job 占用全部 8 卡**。

- 8B bf16 约 17GB，单卡放得下 → 一卡一副本、数据切 8 片（数据并行）
  - 领域线：`infer.gpu_ids: [0..7]`, `workers_per_gpu: 1`
  - 通用线：`CUDA_VISIBLE_DEVICES=0..7` + `torchrun --nproc-per-node 8`, `MODEL_CONCURRENT=1`
- L1 内部顺序：先领域线（几分钟）→ 再通用线（十几分钟），不并行
- 领域裁判（本地 27B）与通用裁判（外部网关）都不占评估机的卡，不参与这把锁

不做 4+4 固定切分：两条线耗时差一个数量级，独占跑完的总墙钟时间短于常驻切分。

### 5.4 幂等与并发

- `request_id` 已有 `succeeded` 结果 → 直接跳过
- 每个 (run_id, step, suite) 独立 `work_dir`，互不污染
- 编排器持文件锁，防止手工命令与常驻进程同时跑同一目录
- checkpoint 完整性二次校验：`config.json` + `model.safetensors.index.json` 声明的分片
  全部存在 + processor/tokenizer 文件齐全，缺一记 `failed`，不静默降级

---

## 六、抽样规则

误差要求 ≤5 分（95% 置信）。按 p≈0.7 计算，误差半宽 = 1.96·√(p(1−p)/n)：

| n | 误差半宽 |
|---|---|
| 200 | ±6.4 |
| **350** | **±4.8** |
| 900 | ±3.0 |

据此定 L1 规模：

| L1 内容 | 条数 | 说明 |
|---|---|---|
| RealWorldQA | 765 全量 | 本来就不多，不抽 |
| MMMU_DEV_VAL | 400 | 留余量 |
| MathVista_MINI | 400 | 留余量 |
| 各 domain suite | 每个 300 | LLM 裁判是连续分，方差小于二值 |

**抽样必须固定且落盘**：

1. 按 suite 内的 `stratify_by`（如 `category` = 单轮/多轮）分层，固定 `seed` 抽取
2. 抽中的 index 列表写入 `samples/<suite>__n<N>__seed<S>.json`
3. 之后**每个 step 都读这个文件**，不重新抽

第 3 条是关键：不固定的话，step 600 和 step 1200 评的不是同一张卷子，
分数差里混着"换了题"的噪声，无法判断是模型变了还是题变了。
抽样文件的哈希进 `fingerprint`，变了就报错中止，不静默继续画曲线。

`eval_tool` 现有的 `infer.limit` 是 `data.head(N)`（`run_infer.py:67`），
取前 N 条而非抽样，需替换为上述机制。

---

## 七、崩点定位

训练不因评估而停，所以报告必须能回答"从哪一步开始崩的、崩在哪个领域"。

对每个 suite 的 primary metric 时间序列：

- 记录历史峰值 `peak_score` 及其 `peak_step`
- 找到第一个满足「相对峰值下降 > `drop_threshold`（默认 3pt）且此后未回升到峰值 −1pt」的 step
- 输出：

```json
{
  "suite": "general_vlm",
  "regression_onset_step": 1200,
  "peak_step": 800,
  "peak_score": 0.6923,
  "current_score": 0.6510,
  "drop": 0.0413,
  "epoch_at_onset": 2.4
}
```

报告里把这个 step 对应的 `epoch` 和当时的 `train_meta.datasets` 一并列出，
让人能直接对应到"训到第几轮、什么配比下开始崩的"。

---

## 八、下一轮数据配比建议

**旋钮 = B：各训练数据集的实际条数。** 输出必须是训练侧能直接抄进配置的条数，
不是"建议加强 XX 领域"这类需要人二次翻译的话。

**时序**：随 `run_finished` 的最终报告一起产出一份；中途仅在触发回归告警时
额外产出一份"当前诊断"，不每个 checkpoint 都出（每个 ckpt 都出只会是同一句话重复 N 遍）。

**输入信号**（只用这四个，不引入其他）：
1. 各领域当前得分
2. 相对 base 的 delta
3. 各领域训练样本量（来自 `train_meta.datasets[].samples`）
4. 通用回归集是否下降

**规则**（阈值可配）：

| 规则 | 条件 | 动作 |
|---|---|---|
| 平台期 | 近 3 个 checkpoint 提升 < 0.5pt | 该领域条数 −10% |
| 爬坡未饱和 | 该领域绝对分最低 且 近 3 个 checkpoint 仍在涨 | 该领域条数 +25% |
| 通用回归 | `general_vlm` 相对 base 下降 > 2pt | 通用数据条数 +30% |
| 过拟合嫌疑 | 领域分涨 且 回归分下降 > 3pt | 该领域 −15%，通用 +30% |
| 幅度兜底 | — | 单项调整不超过 ±30%；调整后总条数保持在原总量 ±10% |

**输出形态**：

```
zb_equip     12000 条 → 10800 条   已达平台期（近3个ckpt提升 0.3pt）
books         3000 条 →  3750 条   仍在爬坡且绝对分最低（0.61）
general_mix   4500 条 →  5850 条   MMMU 相对 base 掉 2.1pt，触发回归告警
─────────────────────────────────
总计         19500 条 → 20400 条   （+4.6%，在 ±10% 兜底内）
```

---

## 九、实施步骤

| 里程碑 | 交付物 | 验收标准 |
|---|---|---|
| **M0 契约冻结** | `contracts/*.example.json` + 本文档 | 训练侧按 A-2/A-3 写出一个真实信号文件，编排器能校验通过 |
| **M1 适配层** | 领域线 / 通用线两个 adapter，输出统一 result schema | 用一次历史 VLMEvalKit 运行结果（`status.json`）跑通归一化，字段无缺 |
| **M2 抽样与趋势** | 分层抽样 + 抽样文件落盘 + `eval_trend.csv` | 同一 checkpoint 跑两次，抽中的 index 与分数完全一致 |
| **M3 编排器** | 扫描 → 校验 → 幂等 → 节流 → 分级队列 → GPU 锁 → 回写 | 手工投 5 个信号（含 1 个重复、1 个坏 checkpoint、3 个 5 分钟内连发），行为符合 §5 |
| **M4 训练侧对接** | LLaMA-Factory `TrainerCallback` | 一次真实训练全程无人工干预，趋势表逐 step 增长 |
| **M5 报告与建议** | HTML 报告 + 崩点定位 + 配比建议 | 用 M4 的真实数据生成一份可直接发出的报告 |

M2 之前不要动 M3——没有稳定的抽样和趋势表，编排器跑起来只会积累不可比的数据。

---

## 十、明确不做的事

- **不做自动早停**，不向训练侧回传任何中断信号。评估只产出报告与建议，训练是否继续由人决定。
- **不算跨领域总分**。只出「领域 × step」矩阵与各自趋势线。把装备 0.75、书籍 0.61、
  通用 0.68 加权成一个数，会直接掩盖"装备涨了但书籍掉了"这种最需要被看见的情况。
- **不删 checkpoint**（存储充足），评估侧不碰训练侧的产物。
- **不改评分口径**：`judge.py`、`judge_rubrics.py`、`score_vqa.py`、`aggregate.py`、
  `report.py`、`prompts/*` 保持不动，这是趋势可比的前提。
- **不把 VLMEvalKit 与 eval_tool 合并**为一个进程。

---

## 十一、边界与故障场景

每一条都是明确行为，不是"视情况而定"。评估侧遇到未列出的异常，一律记 `failed` 并保留现场，
**绝不静默降级、绝不用默认值填坑**——静默填坑会在趋势线上产生一个假的暴跌或假的持平，
比直接失败危险得多。

### 11.1 信号层

| # | 场景 | 评估侧行为 | 需要训练侧配合 |
|---|------|-----------|---------------|
| B1 | `.done` 存在但 `.json` 解析失败 | 重读 3 次（间隔 2s，防共享盘同步延迟），仍失败则记 `failed`，信号移入 `_rejected/`，不重试 | — |
| B2 | `.json` 存在但没有 `.done` | 永不处理。超过 6 小时告警一次「孤儿信号」 | — |
| B3 | 缺必填字段 / `schema_version` 不匹配 | 直接拒收，记 `failed`，错误信息里写明缺哪个字段 | — |
| B4 | `finetuning_type` 与 checkpoint 实际形态不符（声明 full 但目录里只有 adapter） | 记 `failed`，不尝试自动 merge | — |
| B5 | **checkpoint 目录已被轮转删除**（排队期间被 `save_total_limit` 清掉） | 记 `failed`，明确报「checkpoint 已不存在」 | ✅ **硬约束：`save_total_limit` 不设或设得大于队列可能积压的数量。** 存储充足，不要开轮转 |
| B6 | 同一 `request_id` 重复投递 | 已有 `succeeded` 结果 → 跳过；已有 `failed` → 重跑一次 | — |
| B7 | 同一 `run_id` 同一 `step` 再次出现（断点续训重跑了这一段） | 视为新 attempt 正常评估；趋势表同一 (run_id, step) 取 `finished_at` 最新的一条 | — |
| B8 | `step` 回退（续训从更早的 step 重新开始） | 正常接收，趋势表按 step 排序而非到达顺序 | — |
| B9 | 训练机与评估机时钟不同步 | 排序只用 `step`，`saved_at` 仅用于超时判断 | — |
| B10 | 训练异常终止，`run_finished` 一直没来 | 最后一个 checkpoint 信号后 **6 小时**无新信号 → 视为 `interrupted`，出报告，不跑 L3 | — |
| B11 | 多个训练任务并发投信号（集群同时跑几个 run） | GPU 全局锁 → 严格串行；队列按 `run_id` 轮转取任务，防止一个 run 饿死另一个 | — |
| B12 | `run_id` 重复（第二次训练忘了改） | 检测到同一 `run_id` 下 `base_model_path` 或 `train_meta.datasets` 变化 → 拒收并告警 | — |

### 11.2 评估执行层

| # | 场景 | 评估侧行为 |
|---|------|-----------|
| B13 | checkpoint 加载失败（transformers 版本不兼容 Qwen3-VL 等） | 记 `failed`，保留完整 traceback，不重试 |
| B14 | **VLMEvalKit 报 `skip_reason`（数据集缺失、依赖不全）** | 该 benchmark 记 `skipped`，**绝不写成 0 分**。趋势线上是断点，不是暴跌 |
| B15 | 领域裁判（本地 27B）不可用 | 领域线失败、通用线成功 → 整体 `partial`，趋势表该 step 缺领域分，报告标注 |
| B16 | 通用裁判（外部网关）不可用 | 同上，方向相反 |
| B17 | 推理 OOM | 记 `failed` 并附显存快照。80G × 8B 正常不会，但 Video-MME 多帧场景要盯 |
| B18 | 评估进程僵死（卡在某条数据不动） | 超时强杀：L1 60 分钟 / L2 4 小时 / L3 24 小时，记 `failed` |
| B19 | `torchrun` 端口冲突（`MASTER_PORT` 固定 29503） | GPU 全局锁保证同时只有一个 job；手工跑时用不同端口 |
| B20 | 评估中途被 kill（断电、运维重启） | 重启后该 `request_id` 无 `succeeded` 结果 → 重新入队；VLMEvalKit 侧靠 `--reuse` 续跑 |
| B21 | 磁盘写满 | 启动前检查剩余空间，低于阈值直接拒绝执行并告警，不开跑 |

### 11.3 可比性层（最隐蔽的一类）

这一类的共同特点是：**不报错也能跑完，但结果没有意义**。所以全部做成硬失败。

| # | 场景 | 评估侧行为 |
|---|------|-----------|
| B22 | 抽样文件丢失或被改动 | 指纹不匹配 → 中止，不重新抽样。重新抽样等于换卷子 |
| B23 | 同一 `run_id` 内裁判提示词 / rubric / 评估集 TSV 变了 | `judge_fp` / `tsv_sha256` 变化 → 拒绝写入趋势表并报错 |
| B24 | VLMEvalKit 版本变了 | `vlmevalkit_commit` 写入指纹；同一 run 内变化 → 告警并在报告中分段标注 |
| B25 | 某个 step 抽样条数与其他 step 不同 | 拒绝写入趋势表 |
| B26 | 领域集与训练集有重叠 | 数据准备阶段一次性哈希去重，结论写进 suite 配置的 `dedup_note` 字段 |

---

## 十二、提前测试清单

这些**不依赖训练侧、不依赖真实训练**，可以现在就做，目的是在对接前把坑踩完。
本仓库已有 `e2e_check.py` 用假模型 + 假裁判跑通全链路的先例，同一思路扩展。

| # | 测什么 | 怎么测 | 通过标准 | 需要 GPU |
|---|--------|--------|---------|---------|
| **T1** | 通用线适配层 | 拿历史 Video-MME 运行留下的 `status.json` 直接喂给 adapter | 能读出 benchmark / primary_metric / eval_samples / 失败率 / done 状态，无缺字段 | ❌ |
| **T2** | 领域线适配层 | 拿一份现成的 `score_summary.csv` + `judge_detail_all.xlsx` 喂给 adapter | 归一化成统一 result schema，指标与原文件逐项对得上 | ❌ |
| **T3** | 信号校验 | 手工投 6 个坏信号：缺字段 / 坏 JSON / 只有 json 没 .done / checkpoint 路径不存在 / schema_version 错 / run_id 重复 | 6 个全部按 B1-B12 的规则处理，错误信息能定位到具体字段 | ❌ |
| **T4** | 幂等 | 同一 `request_id` 连投 3 次 | 只执行一次，另两次记 `skipped` | ❌ |
| **T5** | 节流 | 5 分钟内连投 3 个不同 step | 只评最新那个，另两个记 `skipped`；`is_final=true` 的不被丢 | ❌ |
| **T6** | 队列公平 | 两个不同 `run_id` 交替投信号 | 轮转取任务，没有一个 run 被饿死 | ❌ |
| **T7** | 抽样一致性 | 同一 checkpoint 跑两次 L1 | 抽中的 index 列表逐条相同，分数完全一致（裁判缓存命中） | ✅ |
| **T8** | 崩溃恢复 | 评估跑到一半 kill -9，重启编排器 | 该任务重新入队并跑完，不产生半截结果 | ✅ |
| **T9** | 裁判断连 | 断开本地 27B，投一个信号 | 整体 `partial`，通用线仍出分，领域分缺失而非 0 | ✅ |
| **T10** | skip ≠ 0 分 | 故意让某个 benchmark 数据集不可读 | 该项 `skipped`，趋势线断点，**不出现 0 分暴跌** | ✅ |
| **T11** | 耗时实测 | 用 base 权重完整跑一次 L1 | 实测总耗时，验证 30 分钟节流是否合理；超了就调抽样条数或节流间隔 | ✅ |
| **T12** | 指纹拦截 | 跑完一次后改一个字的裁判提示词，再跑同一 run | 拒绝写入趋势表并报错，不静默继续 | ✅ |

**执行顺序**：T1-T6 全部不需要 GPU，可以先做完，它们覆盖了编排器的绝大部分逻辑；
T7-T12 需要占卡，集中排一次机时跑完。
其中 **T11 最优先**——它的结果会反过来决定抽样条数和节流间隔这两个已定参数是否要调。
